# Every planted issue is a false-positive category -> zero promotions.
EXPECTED_EMPTY=no
EXPECTED_KIND=range
EXPECTED_OUTCOME=not-flagged
NOTES="Formatting nitpick + missing tests both map to the verbatim FP list; scope discipline drops them. No findings container survivors."
