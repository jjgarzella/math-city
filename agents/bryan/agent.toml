scope = "rig"
dir = "MMPSingularities.jl"
wake_mode = "fresh"
work_dir = ".gc/worktrees/{{.Rig}}/{{.AgentBase}}"
idle_timeout = "4h"
max_active_sessions = 1
pre_start = ["{{.CityRoot}}/packs/gasvillage/assets/scripts/worktree-setup.sh {{.RigRoot}} {{.WorkDir}} {{.AgentBase}} --sync"]
session_live = ["{{.CityRoot}}/packs/gasvillage/assets/scripts/tmux-theme.sh {{.Session}} {{.Agent}} {{.CityRoot}}/packs/gasvillage"]
