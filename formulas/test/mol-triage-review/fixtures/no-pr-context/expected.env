# Local branch, no remote: angle (d) self-skips, angles a/b/c/e run. The clean
# change has no planted defect, so the expected net is zero promotions.
EXPECTED_EMPTY=no
EXPECTED_KIND=branch
EXPECTED_OUTCOME=angle-d-skips-others-run
NOTES="Angle (d) must return exactly 'skipped: no PR context'; no errors; net 0 findings."
