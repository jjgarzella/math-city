# Step 0 stops before the pipeline: empty diff, exit 0.
EXPECTED_EMPTY=yes
EXPECTED_KIND=range
EXPECTED_OUTCOME=skipped-empty
NOTES="Deterministic in step 0; the pipeline never runs. No findings container."
