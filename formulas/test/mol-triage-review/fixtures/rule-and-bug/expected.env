# Two distinct findings, both >= threshold, different root causes -> 2 promoted.
EXPECTED_EMPTY=no
EXPECTED_KIND=range
EXPECTED_OUTCOME=promoted-2
NOTES="rule-adherence flags missing doc comment (quoting AGENTS.md); bug-scan flags xs[0] panic. Not merged (different root cause)."
