# Two angles, one underlying issue -> step 7 merges to a single promoted bead
# bearing both category labels + a merge note; the duplicate wisp is burned.
EXPECTED_EMPTY=no
EXPECTED_KIND=range
EXPECTED_OUTCOME=dedup-1
NOTES="bug-scan (b) + git-history (c) collide on app.go / removed guard. One bead, labels category:bug-scan + category:git-history, dup burned. audit: considered 2, promoted 1, merged 1, burned 0."
